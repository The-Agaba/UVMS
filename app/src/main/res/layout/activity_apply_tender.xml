<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/uvmsBackgroundLight">

    <LinearLayout

        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@color/uvmsBackgroundLight"
        android:elevation="0.5dp"
        android:clipToPadding="false"
        android:layout_margin="8dp"
        android:gravity="center_vertical"
        android:divider="@color/uvmsDivider"
        android:showDividers="middle">

        <!-- Header -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tender Application Form"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/uvmsTextPrimaryLight"
            android:gravity="center"
            android:padding="8dp"
            android:layout_marginBottom="12dp" />

        <!-- Company Name -->
        <EditText
            android:id="@+id/etCompanyName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Company Name *"
            android:padding="12dp"
            android:background="@color/uvmsSurfaceLight"
            android:textColor="@color/uvmsTextPrimaryLight"
            android:textColorHint="@color/uvmsTextSecondaryLight"
            android:layout_marginBottom="10dp" />

        <!-- Contact Person -->
        <EditText
            android:id="@+id/etContactPerson"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Contact Person *"
            android:padding="12dp"
            android:background="@color/uvmsSurfaceLight"
            android:textColor="@color/uvmsTextPrimaryLight"
            android:textColorHint="@color/uvmsTextSecondaryLight"
            android:layout_marginBottom="10dp" />

        <!-- Email -->
        <EditText
            android:id="@+id/etEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Email Address *"
            android:inputType="textEmailAddress"
            android:padding="12dp"
            android:background="@color/uvmsSurfaceLight"
            android:textColor="@color/uvmsTextPrimaryLight"
            android:textColorHint="@color/uvmsTextSecondaryLight"
            android:layout_marginBottom="10dp" />

        <!-- Proposal -->
        <EditText
            android:id="@+id/etProposal"
            android:layout_width="match_parent"
            android:layout_height="150dp"
            android:hint="Proposal / Bid Details *"
            android:gravity="top"
            android:inputType="textMultiLine"
            android:padding="12dp"
            android:background="@color/uvmsSurfaceLight"
            android:textColor="@color/uvmsTextPrimaryLight"
            android:textColorHint="@color/uvmsTextSecondaryLight"
            android:layout_marginBottom="10dp" />

        <!-- Upload Document -->
        <Button
            android:id="@+id/btnUploadDoc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Upload Required Document"
            android:layout_marginTop="16dp"
            android:backgroundTint="@color/uvmsAccentCyan"
            android:textColor="@color/uvmsTextPrimaryDark" />

        <TextView
            android:id="@+id/tvSelectedDoc"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="No document selected"
            android:textSize="14sp"
            android:textColor="@color/uvmsTextSecondaryDark"
            android:layout_marginTop="6dp" />

        <!-- Submit Button -->
        <Button
            android:id="@+id/btnSubmitApplication"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Submit Application"
            android:layout_marginTop="20dp"
            android:backgroundTint="@color/colorAccent"
            android:textColor="@color/uvmsTextPrimaryDark"
            android:padding="12dp" />

    </LinearLayout>
</ScrollView>
