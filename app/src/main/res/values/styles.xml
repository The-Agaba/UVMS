<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
    </style>

    <style name="Card">

    </style>

    <style name="Card.Base">
        <item name="android:layout_margin">8dp</item>
        <item name="android:elevation">3dp</item>
        <item name="cardCornerRadius">12dp</item>
    </style>

    <style name="Btn.Primary" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/uvmsBackgroundLight</item>
        <item name="backgroundTint">@color/uvmsTextPrimaryDark</item>
        <item name="cornerRadius">16dp</item>
    </style>

    <style name="TenderDetailText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>

        <!-- Modern, readable font -->
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#333333</item>
        <item name="android:lineSpacingExtra">2dp</item>

        <!-- Subtle letter spacing for modern feel -->
        <item name="android:letterSpacing">0.02</item>

        <!-- Padding & margins for breathing room -->
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:layout_marginBottom">6dp</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingEnd">4dp</item>
    </style>


    <style name="TenderInputText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">12dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#333333</item>
        <item name="android:background">@drawable/bg_edittext</item>
        <item name="android:layout_marginTop">12dp</item>
    </style>

</resources>
