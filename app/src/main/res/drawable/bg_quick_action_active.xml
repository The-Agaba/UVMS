<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Highlighted background (use your theme’s primary) -->
    <solid android:color="@color/uvmsAccentCyan" />

    <!-- Rounded corners to match -->
    <corners android:radius="16dp" />

    <!-- Bold border to stand out -->
    <stroke
        android:width="2dp"
        android:color="@color/uvmsBlueDark" />

    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
